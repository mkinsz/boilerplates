import _ from 'lodash';

const initialState = {
	results: [],
	offset: 0,
	size: 0,
	total: 0,
	waiting: false
};

export const mspsLog = (state = initialState, action) => {
	switch (action.type) {
		
		default:
			return state;
	}
};
